import React from 'react'

const ResetButton = ({ onClick }) => (
  <button type="button" className="ResetButton btn btn-default" onClick={onClick}>
    Reset
  </button>
)

export default ResetButton
